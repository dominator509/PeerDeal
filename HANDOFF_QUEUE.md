# Handoff Queue

Generated: 2026-08-10
Branch: `retrofit/baseline-v1`
Backup tag: `pre-retrofit-20260613T075234Z`

## Queue

| ID | Source | Category | Tier | Status | Resolution / Blocker |
| --- | --- | --- | --- | --- | --- |
| `LEGACY-GAP-2026-06-13-001` | `REPO_INVENTORY.md` | 2 | T1 | RESOLVED | Added repository inventory and package boundary map. |
| `LEGACY-GAP-2026-06-13-002` | `HANDOFF_QUEUE.md` | 5 | T1 | RESOLVED | Added stable queue for retrofit gaps and deferred production blockers. |
| `LEGACY-GAP-2026-06-13-003` | `PROJECT_STATE.md` | 2 | T1 | RESOLVED | Added current tier, branch, tag, and required gates. |
| `LEGACY-GAP-2026-06-13-004` | `HANDOFF.md` | 5 | T1 | RESOLVED | Added current handoff snapshot for Codex/Claude continuity. |
| `LEGACY-GAP-2026-06-13-005` | `DECISIONS.log` | 5 | T1 | RESOLVED | Added immutable decision trail for this retrofit baseline. |
| `LEGACY-GAP-2026-06-13-006` | `.github/workflows/ci.yml` | 8 | T4 | RESOLVED | CI now activates Melos 8.2.2, matching the workspace dependency and lockfile. |
| `LEGACY-GAP-2026-06-13-007` | `docs/PRODUCTION_READINESS.md` | 4 | T4 | DEFERRED | Remaining native work is other-platform capture, local-network discovery, live peer transport, production database persistence, runtime key/capture validation, and other-platform storage; JSON recovery wipe, app retention coordination, exactly-once per-session close seams, protocol-event adapters, mirrored app session-runtime owners, canonical event-byte decoding, app frame-to-runtime handlers, bounded app source scheduling, route lifecycle source mounting, the app transport provisioner, and Android/Windows app-private recovery-root selection are implemented. Native peer transport implementation, platform source provisioning, database persistence, and other-platform storage remain open. |
| `LEGACY-GAP-2026-06-13-008` | `docs/PRODUCTION_READINESS.md` | 4 | T4 | DEFERRED | Final production UI validation and non-demo product flow validation remain future work. |
| `LEGACY-GAP-2026-08-09-009` | `apps/peerdeal_mobile/android/` | 4 | T4 | RESOLVED | Added the Android host, generic Keystore-backed secure-key implementation, and fail-closed release-signing configuration behind the existing channel contract. |
| `LEGACY-GAP-2026-08-09-010` | `apps/peerdeal_desktop/windows/` | 4 | T4 | RESOLVED | Added the Windows host, generic Credential Manager-backed secure-key implementation, bounded versioned record envelope, and locked channel registration; runtime profile persistence validation remains explicit. |
| `LEGACY-GAP-2026-08-09-011` | `apps/peerdeal_mobile/android/`, `apps/peerdeal_desktop/windows/` | 4 | T4 | DEFERRED | Added Android `FLAG_SECURE` and Windows `SetWindowDisplayAffinity` behind the generic `setBlocking` contract, with app-owned serialized apply/release and fail-closed visual-obscuring fallback; Android/Windows host builds and the dedicated Windows native-host smoke pass, while runtime/device behavior and other-platform capture remain open. |
| `LEGACY-GAP-2026-08-10-012` | `packages/peerdeal_native_bridges/`, `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T18 | RESOLVED | Added the generic app-support directory bridge and wired Android private no-backup storage plus Windows `LocalAppData` into the app-owned recovery factory, with explicit environment-root precedence and fail-closed fallback behavior. |
| `LEGACY-GAP-2026-08-10-013` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T19 | RESOLVED | Production entrypoints now install the app-owned method-channel native readiness loader; unavailable host capabilities render as unavailable and cannot silently bypass readiness-gated routes. |
| `LEGACY-GAP-2026-08-10-014` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T20 | RESOLVED | App-owned local-network bootstrap loaders now split documented `peer@host[:port]` discovery values, validate host/port shape, preserve bare peer IDs, and project safe endpoint metadata onto existing `BootstrapCandidate` fields. |
| `LEGACY-GAP-2026-08-10-015` | `apps/peerdeal_mobile/android/` | 4 | T21 | RESOLVED | Android secure-key persistence now bounds the actual UTF-8 encoded envelope bytes on read and write; the release manifest declares the minimum INTERNET permission for the existing native-network boundary. |
| `LEGACY-GAP-2026-08-10-016` | `packages/peerdeal_core/` | 4 | T22 | RESOLVED | Core command validation now consults the protocol catalog, rejects unsupported command artifacts and protocol versions, and rejects padded or control-character envelope identities before command acceptance. |
| `LEGACY-GAP-2026-08-10-017` | `packages/peerdeal_core/` | 4 | T23 | RESOLVED | Removed the unused starter `CoreCommand`/`CoreEvent` and duplicate reducer/validator contracts; the public core barrel now exposes only protocol-native command/event processing and current deterministic invariants. |
| `LEGACY-GAP-2026-08-10-018` | `packages/peerdeal_variants/` | 4 | T24 | RESOLVED | Added the variant-owned `HoldemCoreProjectionAdapter` and immutable event cursor to connect action, showdown, and settlement state-machine output to catalog-approved protocol events and transactional `CoreReducer` projection without moving Hold'em rules into core. |
| `LEGACY-GAP-2026-08-10-019` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T25 | RESOLVED | Added mirrored `AppHoldemTableSessionRuntime` owners and atomic non-retention event-batch commits so app-local Hold'em lifecycle output is accepted through the existing app session boundary before variant state/cursor advancement. |
| `LEGACY-GAP-2026-08-10-020` | `packages/peerdeal_variants/`, `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T26 | RESOLVED | Added exact remote cursor acceptance, variant-owned Hold'em event reduction, atomic app inbound projection, and optional transport/provisioner wiring so canonical remote Hold'em events reconstruct local variant and core state without moving rules into core. |
| `LEGACY-GAP-2026-08-10-021` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T27 | RESOLVED | Added mirrored app-owned Hold'em route composition, accepted inbound-event surface refresh, and canonical projection transport publishing with explicit partial-send results. Live native peer transport, actual product route/state wiring, and final production UI remain open. |
| `LEGACY-GAP-2026-08-10-022` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T28 | RESOLVED | Added typed Hold'em production-route registration, automatic validated route-map/navigation merge, and native-readiness gating with fail-closed mounting. Product session/state provisioning, native peer transport, and final UI remain open. |
| `LEGACY-GAP-2026-08-10-023` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T29 | RESOLVED | Added mirrored production Hold'em surfaces with bounded projection rendering, transport-backed local actions, canonical publication, and resumable partial-send retry. Product session/state provisioning, native peer transport, durable persistence, device validation, and final UX validation remain open. |
| `LEGACY-GAP-2026-08-10-024` | `apps/peerdeal_mobile/android/`, `apps/peerdeal_desktop/windows/` | 4 | T30 | RESOLVED | Added bounded UDP multicast implementations behind the locked generic native transport channel, with host-private envelope validation, receive queues, lifecycle teardown, and mobile/desktop host build coverage. Device/network reachability, firewall behavior, other-platform transport, and product session wiring remain open. |
| `LEGACY-GAP-2026-08-10-025` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T31 | RESOLVED | Added mirrored app-owned production Hold'em session factories that compose injected canonical state, close-retention adapters, peer identity, existing runtimes, and the default route surface with fail-closed metadata, seat, polling, and cursor/session validation. Real product session/state provisioning and device/network validation remain open. |
| `LEGACY-GAP-2026-08-10-026` | `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T32 | RESOLVED | Added mirrored app-owned production session source/bootstrap contracts, validated resolved-invite to table/session/protocol correlation before factory invocation, and propagated `ResolvedInvite` on successful join/rejoin outcomes. The concrete product source, durable state hydration, local identity provisioning, and device/network validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-027` | `apps/peerdeal_mobile/android/`, `apps/peerdeal_desktop/windows/` | 4 | T33 | RESOLVED | Hardened native transport lifecycle failure paths: Android resolves calls safely across executor teardown/rejection and drains accepted work; Windows cleans up partial Winsock/socket initialization before reporting unavailable. Runtime device, firewall, and cross-device reachability validation remain open. |
| `LEGACY-GAP-2026-08-10-028` | `packages/peerdeal_native_bridges/`, `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T34 | RESOLVED | Added a bounded five-second default deadline to generic secure-key method-channel load/save/delete calls, stable fail-closed timeout results, and mirrored receipt-route guards that reject unavailable export artifacts before native key verification. Native key-store persistence and device validation remain open. |
| `LEGACY-GAP-2026-08-10-029` | `packages/peerdeal_native_bridges/`, `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T35 | RESOLVED | Added a bounded five-second default deadline to generic native transport capability, send, and receive method-channel calls, caller cancellation for app route lifecycle teardown, and stable fail-closed results. Runtime device, firewall, cross-device reachability, and other-platform transport remain open. |
| `LEGACY-GAP-2026-08-10-030` | `packages/peerdeal_native_bridges/`, `apps/peerdeal_mobile/`, `apps/peerdeal_desktop/` | 4 | T36 | RESOLVED | Added a bounded five-second default deadline to generic local-network capability and discovery method-channel calls, caller cancellation through default app bootstrap loaders, and route replacement/disposal cleanup. Real platform discovery, permission behavior, device/network reachability, and other-platform implementations remain open. |
| `LEGACY-GAP-2026-08-10-031` | `apps/peerdeal_mobile/android/` | 4 | T37 | RESOLVED | Hardened Android native multicast transport receiver setup and teardown against close races, released partial socket/multicast-lock resources, and cleared queued transport frames during close. Android device/network reachability, firewall behavior, release signing, and other-platform hosts remain open. |
| `LEGACY-GAP-2026-08-10-032` | `apps/peerdeal_desktop/windows/` | 4 | T38 | RESOLVED | Hardened Windows native multicast transport socket access across method calls, receive-thread startup, and teardown; shutdown invalidates and closes the socket before joining the receiver and clears queued frames. Windows profile/network reachability, Android device validation, release signing, and other-platform hosts remain open. |
| `LEGACY-GAP-2026-08-10-033` | `apps/peerdeal_mobile/android/` | 4 | T39 | RESOLVED | Hardened Android secure-key worker lifecycle around Flutter engine teardown; queued storage work fails closed after handler closure and late main-looper results never return native key material. Android device persistence/capture, release signing, and other-platform hosts remain open. |
| `LEGACY-GAP-2026-08-10-034` | `apps/peerdeal_desktop/windows/` | 4 | T40 | RESOLVED | Hardened Windows secure-key and capture method-channel teardown by unregistering handlers during destruction, preventing shutdown dispatch from targeting released native owners. Windows runtime/profile validation, Android device validation, release signing, and other-platform hosts remain open. |
| `LEGACY-GAP-2026-08-10-035` | `apps/peerdeal_mobile/lib/session/`, `apps/peerdeal_desktop/lib/session/` | 4 | T41 | RESOLVED | Added mirrored app-owned `AppHoldemProductionSessionBootstrapRoute` adapters. Product callers can pass a resolved invite through route arguments, invoke the existing validated bootstrap, and mount its production route; missing arguments, source failures, and route-path mismatches fail closed. Durable state hydration, local identity, device validation, and final navigation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-036` | `apps/peerdeal_mobile/lib/main.dart`, `apps/peerdeal_desktop/lib/main.dart` | 4 | T42 | RESOLVED | Added an optional opaque payload to mirrored app-shell navigation entries and forwarded it through default-home `RouteSettings.arguments`. Product callers can now launch the T41 route adapter with a resolved invite without moving session semantics into the shell; source hydration, local identity, device validation, and final UX remain open. |
| `LEGACY-GAP-2026-08-10-037` | `apps/peerdeal_mobile/lib/join_flow/`, `apps/peerdeal_desktop/lib/join_flow/`, `apps/peerdeal_mobile/lib/main.dart`, `apps/peerdeal_desktop/lib/main.dart` | 4 | T43 | RESOLVED | Preserved only accepted, identity-safe resolved invites through mounted join outcomes and added an optional post-frame `JoinFlowReadyHandler` wired through both app runtimes. Product callers can now hand a successful join/rejoin to the T41 route adapter; rejected outcomes and callback failures fail closed. Durable source hydration, local identity provisioning, and native validation remain open. |
| `LEGACY-GAP-2026-08-10-038` | `apps/peerdeal_mobile/lib/session/`, `apps/peerdeal_desktop/lib/session/` | 4 | T44 | RESOLVED | Bounded product-owned session source hydration with a configurable positive timeout and a five-second default. Mounted bootstrap routes now render a loading surface while hydration is pending and fail closed only after completion errors, including timeout. Durable source hydration, local identity provisioning, and native validation remain open. |
| `LEGACY-GAP-2026-08-10-039` | `apps/peerdeal_mobile/lib/session/`, `apps/peerdeal_desktop/lib/session/` | 4 | T45 | RESOLVED | Added an optional source cancellation signal to the mirrored production-session bootstrap contract. Route replacement and disposal now cancel the app-owned wait, and the bootstrap cancels its timeout timer on every terminal outcome. The concrete product source remains responsible for canceling underlying persistence or network work. |
| `LEGACY-GAP-2026-08-10-040` | `apps/peerdeal_mobile/lib/main.dart`, `apps/peerdeal_desktop/lib/main.dart`, mirrored `session/` registration and app-shell tests | 4 | T46 | RESOLVED | Added optional app-shell registration for the existing production-session bootstrap route. Both shells merge the route and native-readiness gate, and accepted join/rejoin outcomes use a cached default handoff when no explicit handler is supplied; explicit handlers still override. Product source/state hydration, local identity, persistence, and device validation remain open. |
| `LEGACY-GAP-2026-08-10-041` | mirrored app `session/` registration files and app-shell tests | 4 | T47 | RESOLVED | Added `AppHoldemProductionSessionBootstrapRouteRegistration.fromSource(...)` in both shells. A product-owned source can now be assembled with the existing bootstrap, factory, and timeout at one app boundary before the registered join handoff; state, identity, persistence, and native work remain source-owned. |
| `LEGACY-GAP-2026-08-10-042` | mirrored app `main.dart`, `session/`, and app-shell tests | 4 | T48 | RESOLVED | Added `AppHoldemProductionSessionConfiguration.fromSource(...)` to both runtimes. The shell derives one stable source-backed registration for route merging, readiness, and default join handoff, while rejecting simultaneous explicit and configured registrations. Concrete source provisioning, state serialization, identity, persistence, device/network validation, and final UX remain open. |
| `LEGACY-GAP-2026-08-10-043` | `apps/peerdeal_mobile/android/app/build.gradle.kts` | 4 | T49 | RESOLVED | Android Gradle release tasks now fail before artifact assembly unless all four operator-owned signing values are present, unpadded, control-free, and backed by an existing keystore file. Debug builds remain available; operator signing credentials and real-device validation remain external. |
| `LEGACY-GAP-2026-08-10-044` | `packages/peerdeal_ui_kit/`, mirrored app Hold'em surfaces and tests | 4 | T50 | RESOLVED | Shared action controls now expose explicit labels/tap semantics, fact rows stack safely below 360px, and mirrored production Hold'em surfaces render human-readable phase/round/actor state with an accessible seats heading. Final visual/device UX validation remains external. |
| `LEGACY-GAP-2026-08-10-045` | `packages/peerdeal_ui_kit/lib/src/app_shell/peer_deal_action_button.dart` and focused UI test | 4 | T51 | RESOLVED | Shared action controls now own focus, expose focus semantics, request focus on pointer activation, and bind Enter, numpad Enter, and Space to the same action with a stable focus outline. Final device/accessibility validation remains external. |
| `LEGACY-GAP-2026-08-10-046` | `packages/peerdeal_ui_kit/lib/src/app_shell/peer_deal_action_button.dart` and focused UI test | 4 | T52 | RESOLVED | Shared action controls now guarantee a 48 logical-pixel minimum interactive height while preserving the stable focus outline. Device, text-scale, and final accessibility validation remain external. |
| `LEGACY-GAP-2026-08-10-047` | mirrored `apps/peerdeal_mobile/lib/session/` and `apps/peerdeal_desktop/lib/session/` production table surfaces and route tests | 4 | T53 | RESOLVED | Fold, Call/Check, and All-in actions now use the configured local seat instead of hard-coded seat zero; canonical outbound event tests lock non-zero local-seat attribution in both app shells. |
| `LEGACY-GAP-2026-08-10-048` | `packages/peerdeal_core/lib/src/models/table_state.dart` and invariant tests | 4 | T54 | RESOLVED | Added strict `TableState.fromJson(...)` hydration matching the existing `toJson()` shape, rejecting malformed primitive fields, unknown phases, and non-string metadata keys. Full product source and durable product persistence wiring remain integration-owned. |
| `LEGACY-GAP-2026-08-10-049` | `packages/peerdeal_variants/lib/src/holdem/holdem_hand_state.dart` and focused variant tests | 4 | T55 | RESOLVED | Added strict JSON serialization and hydration for `HoldemHandState` and `HoldemSeatState`, including enum, nested object, collection, and nullable-field validation. Product source, database wiring, and local identity remain integration-owned. |
| `LEGACY-GAP-2026-08-10-050` | `packages/peerdeal_variants/lib/src/holdem/holdem_core_projection_adapter.dart` and focused cursor tests | 4 | T56 | RESOLVED | Added strict `HoldemEventCursor` JSON serialization and hydration for scope, sequence, hash-chain, actor, and last-event state. Hydration requires caller-owned event-id, timestamp, and optional hash factories; product persistence and identity wiring remain integration-owned. |
| `LEGACY-GAP-2026-08-10-051` | `packages/peerdeal_variants/` and mirrored app `session/` sources/tests | 4 | T57 | RESOLVED | Added typed `HoldemStateSnapshot` hydration and mirrored `AppPersistedHoldemProductionSessionSource` adapters over the existing recovery store. Missing, wrong-version, wrong-scope, and post-snapshot suffix data fail closed; input factories, event factories, product database wiring, local identity, and suffix replay remain integration-owned. |
| `LEGACY-GAP-2026-08-10-052` | `packages/peerdeal_variants/` and mirrored app persisted-session sources/tests | 4 | T58 | RESOLVED | Added atomic deterministic recovery-suffix replay through cursor acceptance, `peerdeal_core` reduction, and hand-scoped Hold'em reduction. Valid suffixes hydrate the advanced state; tampered or unsupported events fail closed without partial state. Product database wiring, local identity, and native/device validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-053` | mirrored app `session/` secure-key identity adapters and tests | 4 | T59 | RESOLVED | Added app-owned local peer identity load, save, and provision contracts over the generic secure-key bridge. One active identity is reused, missing identity is provisioned with a secure random default, and unavailable, invalid, or ambiguous records fail closed. Wiring the identity into the concrete product source and route policy remains integration-owned. |
| `LEGACY-GAP-2026-08-10-054` | mirrored app `session/` persisted-source composition and focused tests | 4 | T60 | RESOLVED | Added app-owned factories that provision or reuse local identity and map it into the existing persisted production-session input with caller-owned route, remote-peer, local-seat, and close-event policy. Database selection, remote-peer discovery, and native runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-055` | mirrored app `session/` local identity provisioners and focused tests | 4 | T61 | RESOLVED | Added single-flight first-use identity provisioning so concurrent callers share one load/generate/save operation; failed operations release the guard for retry. Cross-process/device runtime validation remains external. |
| `LEGACY-GAP-2026-08-10-056` | mirrored app `session/` local identity provisioners and focused tests | 4 | T62 | RESOLVED | Added fail-closed read-back verification after generated identity persistence. A provisioner now succeeds only when native storage reloads the same peer ID, detecting competing writers or inconsistent storage; cross-process locking remains external. |
| `LEGACY-GAP-2026-08-10-057` | mirrored app `join_flow/`, `main.dart`, and `session/` sources/routes/tests | 4 | T63 | RESOLVED | Added typed first-join `JoinFlowSessionContext` propagation from selected bootstrap peer plus governance-assigned seat into the context-aware persisted production source and route. Invite-only callbacks remain compatible; rejoin peer binding, real product persistence/transport provisioning, and runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-058` | mirrored app `join_flow/` models, orchestrators, fakes, routes, and tests | 4 | T64 | RESOLVED | Added optional governance-assigned remote peer binding to accepted rejoin results and propagated it into the typed session context; missing binding fails closed while first join remains bootstrap-selected. Concrete product source/persistence, native transport reachability, and runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-059` | mirrored app `session/` configuration and persisted-source tests | 4 | T65 | RESOLVED | Added `AppHoldemProductionSessionConfiguration.fromPersistedLocalIdentity(...)` to compose native local identity provisioning, existing recovery persistence, caller-owned route policy, and event factories into the validated bootstrap route. Production database/state provisioning and runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-060` | mirrored app `session/` configuration and persisted-source tests | 4 | T66 | RESOLVED | Configuration factories now reject non-positive source-load timeouts before route assembly or native local-identity provisioning; mirrored tests prove invalid configuration causes no secure-key mutation. Product database/state provisioning and runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-061` | mirrored app `session/` persisted-source and route-policy tests | 4 | T67 | RESOLVED | Persisted-session route policy now validates path, navigation label, remote peer identity, and positive seat before native local-identity provisioning; mirrored tests prove invalid route policy causes no secure-key mutation. Product database/state provisioning and runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-062` | mirrored app `session/` persisted-source configuration and tests | 4 | T68 | RESOLVED | Production persisted-session configuration now defers native local-identity provisioning until after invite-scoped snapshot validation and recovery replay; missing or rejected recovery state causes no secure-key mutation. Product database/state provisioning and runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-063` | mirrored app `session/` persisted-source cancellation and tests | 4 | T69 | RESOLVED | Persisted invite and session-context loads now honor route cancellation before recovery access and around lazy identity provisioning; cancelled loads fail closed without secure-key mutation. Product database/state provisioning and runtime validation remain integration-owned. |
| `LEGACY-GAP-2026-08-10-064` | `packages/peerdeal_native_bridges/` capture method-channel bridge and tests | 4 | T70 | RESOLVED | Generic capture capability and blocking calls now use a bounded five-second default deadline, reject non-positive timeout configuration, and return stable fail-closed timeout results. Runtime/device capture behavior remains external. |
| `LEGACY-GAP-2026-08-10-065` | `packages/peerdeal_native_bridges/` secure storage and mirrored app session identity seams | 4 | T71 | RESOLVED | Added an additive per-call secure-storage cancellation capability and propagated persisted-route cancellation through app identity load/save/read-back paths. The Dart wait fails closed; an already-dispatched host mutation remains host-owned and cannot be retroactively withdrawn. |
| `LEGACY-GAP-2026-08-10-066` | mirrored app demo receipt loaders, verifiers, presenters, and routes | 4 | T72 | RESOLVED | Added additive receipt verification cancellation through native key-ring loading and mounted route replacement/disposal. Legacy secure-key bridges remain supported; native host rollback, runtime device validation, and product persistence remain external or integration-owned. |
| `LEGACY-GAP-2026-08-10-067` | mirrored app transport source, session factory, and provisioner lifecycle | 4 | T73 | RESOLVED | Added route/disposal cancellation for transport session loading and polling, retaining in-flight drain registration until settlement so cancellation cannot create overlapping native drains. Native reachability, host rollback, runtime validation, and product persistence remain external or integration-owned. |
| `LEGACY-GAP-2026-08-10-068` | Android and Windows native transport identity decoding | 4 | T74 | RESOLVED | Native host frame decoders now reject malformed UTF-8 and C1-control-bearing session/peer identities before queueing or sending. Device/network reachability and runtime host validation remain external. |
| `LEGACY-GAP-2026-08-10-069` | Android and Windows local-network host registration | 4 | T75 | RESOLVED | Registered the locked local-network method channel on both hosts and added bounded active-interface capability/hint reporting. Peer discovery remains fail-closed with no fabricated endpoints because the repository has no discovery advertisement protocol. |
| `LEGACY-GAP-2026-08-10-070` | Capture bridge and mounted receipt-route lifecycle cancellation | 4 | T76 | RESOLVED | Added additive cancellable capture capability/action interfaces, bounded method-channel cancellation, and mirrored receipt presenter/coordinator propagation on route replacement and disposal. Native calls already dispatched remain host-owned; runtime/device capture validation remains external. |
| `LEGACY-GAP-2026-08-10-071` | App-support directory method-channel deadline and lifecycle cancellation | 4 | T77 | RESOLVED | Added a bounded five-second app-storage lookup with additive cancellation and propagated it through mirrored native recovery-store factories. Missing or cancelled native directory lookup still fails closed without creating a persistence factory. |
| `LEGACY-GAP-2026-08-10-072` | App-shell native-readiness aggregation lifecycle cancellation | 4 | T78 | RESOLVED | Added additive cancellable local-network and native-transport capability interfaces, forwarded route cancellation through mirrored readiness loaders, and cancelled stale readiness work on loader replacement or app disposal. Existing base bridges remain compatible; already-dispatched host calls and runtime/device validation remain external. |
| `LEGACY-GAP-2026-08-10-073` | Native host compilation absent from CI | 4 | T79 | RESOLVED | Added separate Android debug APK and Windows debug host CI jobs. These compile gates catch host integration regressions without claiming release signing or runtime/device validation. |
| `LEGACY-GAP-2026-08-10-074` | Android release-signing guard absent from CI | 4 | T80 | RESOLVED | Added an expected-failure CI check proving a credential-free Android release build stops at the Gradle signing guard. The check does not use or expose operator credentials and does not claim a signed release artifact. |
| `LEGACY-GAP-2026-08-10-075` | Android signing CI check accepted arbitrary failures | 4 | T81 | RESOLVED | The release-signing CI check now blanks all four signing variables and requires the exact Gradle signing diagnostic, preventing unrelated Android build failures from satisfying the negative guard. |
| `LEGACY-GAP-2026-08-10-076` | Mounted join-flow teardown did not cancel active orchestration | 4 | T82 | RESOLVED | Mirrored join routes now cancel replaced/disposed outcomes, orchestrators stop between pre-commit stages, and native bootstrap forwards the signal to cancellable local-network bridges. Already-dispatched adapter or governance calls remain owner-hosted; runtime/device validation remains external. |
| `LEGACY-GAP-2026-08-11-077` | Concurrent receipt key provisioning raced native writes | 4 | T83 | RESOLVED | Mirrored app receipt key-ring provisioners now single-flight concurrent ensure calls, preventing duplicate key generation and divergent in-memory rings while preserving retry behavior after completion. |
| `LEGACY-GAP-2026-08-11-078` | Receipt key writes were trusted without read-back verification | 4 | T84 | RESOLVED | Mirrored receipt key-ring provisioners now reload native storage after successful key creation and fail closed unless both active signing and encryption keys match exactly. |
| `LEGACY-GAP-2026-08-11-079` | Mounted receipt export provisioning ignored route cancellation | 4 | T85 | RESOLVED | Mirrored app shells now expose a cancellation-aware receipt export callback; route teardown propagates through export, key provisioning, key writes, and cancellable native secure storage while the legacy callback remains compatible. |
| `LEGACY-GAP-2026-08-11-080` | Windows secure-key host read-modify-write race across app processes | 4 | T86 | RESOLVED | Windows secure-key load/save/delete now acquire a per-namespace Local named mutex with a bounded five-second wait, serializing PeerDeal host access while preserving generic credential storage and fail-closed results. T88 adds revision-aware conditional mutations for stale-writer detection; runtime/device validation remains external. |
| `LEGACY-GAP-2026-08-11-081` | Android secure-key handler read-modify-write race across app processes | 4 | T87 | RESOLVED | Android secure-key load/save/delete now use encrypted private files plus a hash-named lock file with bounded five-second `tryLock` retries; legacy preference records migrate under the lock without changing AES-GCM or Keystore contracts. T88 adds revision-aware conditional mutations for stale-writer detection; runtime/device validation remains external. |
| `LEGACY-GAP-2026-08-11-082` | App secure-key read-modify-write lacked compare-and-swap/version semantics | 4 | T88 | RESOLVED | Generic snapshots now carry a nonnegative namespace revision, and additive conditional save/delete methods return explicit conflicts. Mirrored receipt and local-identity provisioners pass expected revisions and recover competing valid records. Android encrypted file envelopes and Windows Credential Manager v2 envelopes persist revisions while legacy methods remain compatible. |
| `LEGACY-GAP-2026-08-11-083` | Windows native host runtime channel coverage was build-only | 4 | T89 | RESOLVED | Added a dependency-free Windows smoke target and executed the built host directly. App storage, capture capability/enable/release, local-network capability/discovery, transport capability/receive, and secure-key save/read-back/CAS/delete all passed. Multicast send returned the host's stable failure and remains firewall/network validation work. |
| `LEGACY-GAP-2026-08-11-084` | Android and Windows native transport send payloads disagreed with the locked Dart channel shape | 4 | T90 | RESOLVED | Both native hosts now unwrap the nested `frame` map emitted by `MethodChannelNativeTransportBridge.sendFrame`. Windows also selects an operational IPv4 multicast interface by adapter metric and uses it for membership and sends. Direct Windows runtime smoke now passes transport send and receive; Android debug APK compilation passes. Cross-device reachability and Android runtime validation remain external. |
| `LEGACY-GAP-2026-08-11-085` | Android multicast transport relied on OS-default interface selection | 4 | T91 | RESOLVED | Android now selects an operational non-loopback IPv4 multicast interface, prefers Wi-Fi/Ethernet deterministically, assigns it to outbound multicast sockets and the receiver, and fails closed when no usable interface exists. Android debug APK compilation passes; Android device and cross-device runtime validation remain external. |
| `LEGACY-GAP-2026-08-11-086` | Windows native host runtime smoke was not enforced by CI | 4 | T92 | RESOLVED | Added a bounded repository-owned PowerShell runner and wired CI to build and execute the existing public-contract smoke target. The runner captures host diagnostics, requires the stable pass marker, fails on nonzero exit, and terminates timed-out hosts. Local execution passes all smoke checkpoints; firewall, device, and cross-device validation remain external. |
| `LEGACY-GAP-2026-08-11-087` | App-owned persisted Hold'em composition had no stable recovery/native factory boundary | 4 | T93 | RESOLVED | Added mirrored configuration factories that compose the existing recovery-store factory, native local-identity provisioner, persisted source, caller-owned route policy, and deterministic event/replay dependencies. They fail closed on unavailable roots or invalid policy. Product snapshot selection/writing, startup invocation, native reachability, and device validation remain separate integration or operator work. |
| `LEGACY-GAP-2026-08-11-088` | No app-owned writer for canonical typed Hold'em recovery snapshots | 4 | T94 | RESOLVED | Added mirrored snapshot writers that validate scope, cursor sequence, and event hash, create a canonical hashed `HoldemStateSnapshot` envelope, and delegate the existing recovery store. Factory results expose the writer over the same store. Product state selection, event-log policy, database persistence, startup invocation, and native/device validation remain separate. |
| `LEGACY-GAP-2026-08-11-089` | JSON recovery store hydrate-modify-write operations were not serialized across processes | 4 | T95 | RESOLVED | Added a stable per-scope OS file lock around JSON recovery-store reads, writes, and wipes. Lock handles close on all paths so process termination releases the advisory lock; lock failures fail closed. The public contract is unchanged, and production database replacement plus platform/runtime validation remain open. |
| `LEGACY-GAP-2026-08-11-090` | No app-owned event-log plus typed-snapshot persistence seam | 4 | T96 | RESOLVED | Added mirrored persistence writers that validate a caller-supplied event suffix, append it before the canonical typed snapshot checkpoint, reject retention events before storage, and expose durable-suffix warnings on checkpoint failure. Configuration results expose the writer over the same recovery store; product state, event identity, snapshot IDs, startup, database, and device policy remain caller-owned. |
| `LEGACY-GAP-2026-08-11-091` | Direct production-source consumers could bypass dynamic rejoin peer/seat validation | 4 | T97 | RESOLVED | Mirrored `AppPersistedHoldemProductionSessionRoutePolicy.buildInput(...)` now revalidates caller-supplied peer identity and local-seat overrides before constructing session input, preserving the bootstrap's fail-closed first-join/rejoin metadata boundary. Product state, route policy, database, and runtime validation remain separate. |
| `LEGACY-GAP-2026-08-11-092` | Invalid checkpoint input could leave a durable event suffix behind | 4 | T98 | RESOLVED | Mirrored app persistence writers now preflight snapshot identity, metadata, scope/cursor/hash consistency, and typed Hold'em state before appending an event suffix, preventing invalid checkpoint input from leaving durable events behind while retaining durable-suffix replay on genuine checkpoint failure. Product state, event identity, snapshot IDs, database, and runtime validation remain caller-owned. |
| `LEGACY-GAP-2026-08-11-093` | Mixed cancellation could clear a newer local identity single-flight operation | 4 | T99 | RESOLVED | Mirrored local-peer identity provisioners now clear only the exact tracked non-cancellable operation, preventing a completed cancellable call from clearing a newer single-flight operation while preserving caller cancellation propagation. Multi-process/native/device validation remains external. |
| `LEGACY-GAP-2026-08-11-094` | Receipt artifact and cipher processing accepted unbounded input sizes | 4 | T100 | RESOLVED | `peerdeal_receipts` now shares bounded encoded-body, decoded-body, payload, ciphertext, and nonce limits across export encoding, export inspection, and the HMAC cipher; oversized input/output fails closed before unbounded base64, JSON, or keystream work. Native key storage, device validation, and release signing remain separate. |
| `LEGACY-GAP-2026-08-11-095` | JSON recovery persistence accepted unbounded file sizes | 4 | T101 | RESOLVED | `JsonFileRecoveryPersistenceStore` now enforces a positive configurable file-size cap (4 MiB by default) before JSON decode and before durable replacement; oversized input/output returns `ERR_RECOVERY_PERSISTENCE_FILE_TOO_LARGE` without writing or hydrating state. Production database replacement and platform validation remain separate. |
| `LEGACY-GAP-2026-08-11-096` | Generic native bridge decoders materialized unbounded platform collections and fields | 4 | T102 | RESOLVED | `peerdeal_native_bridges` now applies shared bounds before iterating transport frames, discovery entries, and secure-key records; frame payloads, identities, discovery values, key fields, and diagnostics are UTF-8/byte bounded, and oversized inputs fail closed. Native host, device, cross-platform, and network runtime validation remain separate. |
| `LEGACY-GAP-2026-08-11-097` | Generic app-storage and capture bridge decoders accepted unbounded platform strings | 4 | T103 | RESOLVED | `peerdeal_native_bridges` now bounds app-storage paths to 4096 UTF-8 bytes and capture notes/warnings to 512 UTF-8 bytes; oversized values fail closed without reaching app models or presenters. Native host, device, cross-platform, and runtime validation remain separate. |
| `LEGACY-GAP-2026-08-11-098` | Native local-network hosts materialized unbounded interface state before the Dart bridge boundary | 4 | T104 | RESOLVED | Android now caps interface enumeration at 64 entries; Windows rejects adapter buffers above 1 MiB and caps adapter traversal at 64 and unicast-address scans at 256. Android/Windows host builds and direct Windows runtime smoke pass; device/network reachability remains separate. |
| `LEGACY-GAP-2026-08-11-099` | Native transport hosts independently materialized unbounded interface state | 4 | T105 | RESOLVED | Android transport now caps multicast-interface enumeration at 64 entries and address scans at 256; Windows transport rejects adapter buffers above 1 MiB and caps adapter/address traversal at 64/256. Android/Windows host builds and direct Windows runtime smoke pass; device/network reachability remains separate. |
| `LEGACY-GAP-2026-08-11-100` | Recovery persistence accepted unbounded event windows and bypassed the canonical event byte bound | 4 | T106 | RESOLVED | `peerdeal_sync` now applies configurable event-count and per-event byte limits to in-memory and JSON recovery stores, defaulting to 4,096 events and the protocol codec's 64 KiB event bound. Oversized batches, persisted windows, and events fail closed before state mutation with stable fatal conflicts; production database replacement and platform/runtime validation remain separate. |
| `LEGACY-GAP-2026-08-11-101` | Privacy diagnostics scrubbing recursively materialized unbounded maps, lists, depth, text, and diagnostic collections | 4 | T107 | RESOLVED | `DefaultDiagnosticsScrubber` now bounds maps and lists at 64 entries, nested depth at 8, text at 512 UTF-8 bytes, and protocol diagnostics at 64 items, emitting stable truncation markers while preserving redaction. App rendering, production database replacement, and platform/runtime validation remain separate. |
| `LEGACY-GAP-2026-08-11-102` | Provider-proof normalization copied unbounded raw structures and proof metadata before verification | 4 | T108 | RESOLVED | `DefaultProviderProofNormalizer` now bounds provider identity/reference text, maps, lists, nesting, nodes, and canonical UTF-8 proof bytes through `DealProofLimits`; unsupported/non-finite values and overflow fail closed, and normalized/raw views share one immutable bounded payload. Provider implementations, product wiring, and runtime/device validation remain separate. |
| `LEGACY-GAP-2026-08-11-103` | Canonical JSON recursively materialized unbounded values before event wire-size enforcement | 4 | T109 | RESOLVED | `peerdeal_protocol` now uses bounded deterministic canonical JSON writing with map/list, depth, text, node, and encoded-byte limits; event encode and decode validation share the configured wire-byte cap and fail closed on unsupported or non-string values. Protocol schema semantics, platform runtime, product persistence, and release signing remain separate. |
| `LEGACY-GAP-2026-08-11-104` | Receipt artifact decoding accepted structurally unbounded JSON after byte caps and top-level map checks | 4 | T110 | RESOLVED | `OpaqueExportDecoder` now validates artifact-body and decrypted-payload JSON through bounded canonical protocol serialization using receipt-owned decoded-body/payload byte limits before receipt shape inspection. Existing signature, cipher, opacity, and authorization semantics remain unchanged; platform key storage and runtime validation remain separate. |
| `LEGACY-GAP-2026-08-11-105` | Typed core and Hold'em state hydration accepted structurally unbounded maps/lists after callers materialized JSON | 4 | T111 | RESOLVED | `TableState`, `HoldemSeatState`, `HoldemHandState`, `HoldemEventCursor`, and `HoldemStateSnapshot` now validate input through the existing bounded canonical protocol serializer before typed field or collection materialization. Core truth remains in `peerdeal_core`, variant rules remain in `peerdeal_variants`, and product persistence/device validation remain separate. |
| `LEGACY-GAP-2026-08-11-106` | Direct protocol envelope hydration and file-backed snapshot recovery bypassed bounded JSON structure validation | 4 | T112 | RESOLVED | `EventEnvelope.fromJson` and `SnapshotEnvelope.fromJson` now validate full materialized JSON through the bounded canonical protocol serializer before field access; file-backed recovery therefore fails closed on structurally oversized persisted snapshots. Protocol schema, sync ownership, and product persistence policy remain unchanged. |
| `LEGACY-GAP-2026-08-11-107` | Replay traversed an unbounded raw event list before event-window validation | 4 | T113 | RESOLVED | `EventWindowValidator` now applies a configurable positive event-count limit, defaulting to 4,096, and `BasicReplayEngine` checks the raw request before protocol, scope, range, selection, or projection work. Oversized input returns `ERR_REPLAY_EVENT_WINDOW_TOO_LARGE`; selected windows remain validated after filtering. Product persistence and runtime/device validation remain separate. |
| `LEGACY-GAP-2026-08-11-108` | Replay anchor hashing and snapshot suffix planning could exceed the request bound or escape as uncaught failures | 4 | T114 | RESOLVED | Replay now short-circuits oversized requests before secondary validation, anchor hashing raises canonical list/node limits to the configured bounded event window, direct suffix/anchor helpers enforce the same default limit, and `BasicReplayEngine` returns stable selection or anchor failure mismatches. Protocol, product persistence, and runtime/device validation remain separate. |
| `LEGACY-GAP-2026-08-11-109` | Direct sync conflict detection and snapshot application traversed caller-provided event lists without the persistence window cap | 4 | T115 | RESOLVED | `BasicConflictDetector` and `BasicSnapshotApplier` now reject oversized in-memory recovery event windows before protocol, scope, sequence, or projector traversal using the shared configurable 4,096-event default and `ERR_RECOVERY_EVENT_COUNT_TOO_LARGE`. Durable persistence remains behind its existing store-specific validation and codes. |
| `LEGACY-GAP-2026-08-11-110` | Direct sync processors accepted event objects without the protocol codec's byte and canonical-structure boundary | 4 | T116 | RESOLVED | `BasicConflictDetector` and `BasicSnapshotApplier` now validate each direct event through the existing `EventEnvelopeCodec` before protocol, scope, sequence, or projector work, using the shared 64 KiB default and canonical structure limits. Oversized and unencodable events return `ERR_RECOVERY_EVENT_TOO_LARGE` or `ERR_RECOVERY_EVENT_INVALID`. |
| `LEGACY-GAP-2026-08-11-111` | Direct sync processors accepted snapshot objects without the bounded canonical structure and encoded-byte boundary | 4 | T117 | RESOLVED | `BasicConflictDetector` and `BasicSnapshotApplier` now validate direct snapshots through bounded canonical JSON before protocol, scope, or snapshot/suffix projection work, using the shared 4 MiB default and protocol structure limits. Oversized and unencodable snapshots return `ERR_RECOVERY_SNAPSHOT_TOO_LARGE` or `ERR_RECOVERY_SNAPSHOT_INVALID`; the JSON store shares the same default constant. |
| `LEGACY-GAP-2026-08-11-112` | Recovery persistence scope identities were unbounded before in-memory indexing and base64url filename generation | 4 | T118 | RESOLVED | `RecoveryPersistenceScope` now enforces a shared 180-byte UTF-8 storage-key bound. In-memory and JSON stores reject oversized scopes with `ERR_RECOVERY_PERSISTENCE_SCOPE_INVALID` before mutation or file creation. |
| `LEGACY-GAP-2026-08-11-113` | Direct sync requests bypassed shared recovery scope identity validation | 4 | T119 | RESOLVED | `BasicConflictDetector` and `BasicSnapshotApplier` now validate direct table/session/protocol scope identities through `RecoveryPersistenceScope` before event, snapshot, or projector work. Invalid requests return `ERR_RECOVERY_SCOPE_INVALID` or `ERR_SNAPSHOT_APPLY_SCOPE_INVALID`. |
| `LEGACY-GAP-2026-08-11-114` | Network routing and confidence services materialized unbounded caller-provided peer collections | 4 | T120 | RESOLVED | `peerdeal_network` now applies shared 32-peer-id, 32-candidate, and 64-peer-metric bounds before materialization. Overflow fails closed as empty bootstrap candidates, unresolved path selection, unsafe confidence, or unsafe primary election. |
| `LEGACY-GAP-2026-08-11-115` | Hold'em showdown materialized caller-provided seats beyond the locked launch invariant | 4 | T121 | RESOLVED | `HoldemShowdownEvaluator` now rejects more than the shared nine-seat limit before sorting or card expansion with `ERR_HOLDEM_SHOWDOWN_SEAT_COUNT`; `HoldemAdapter` identity and config validation reuse the same limit. |
| `LEGACY-GAP-2026-08-11-116` | Hold'em settlement passed unbounded commitment collections into core side-pot construction | 4 | T122 | RESOLVED | `ShowdownSettlementProjector` now applies the shared nine-seat commitment bound before core traversal for contested and uncontested paths; overflow fails closed with `ERR_HOLDEM_SETTLEMENT_PROJECT_COMMITMENT_COUNT`. |
| `LEGACY-GAP-2026-08-11-117` | Hold'em showdown projection materialized oversized result, slice, and contested seat-ID collections | 4 | T123 | RESOLVED | `ShowdownEvaluationResult` now bounds direct result, slice-map, and per-slice seat-ID projection inputs; overflow carries stable warnings through `ShowdownSliceWinnerProjection` and blocked settlement results. |
| `LEGACY-GAP-2026-08-11-118` | Core pot settlement materialized unbounded commitments and winner collections | 4 | T124 | RESOLVED | `SidePotBuilder` and `PotEngine` now apply variant-agnostic 64-entry bounds before side-pot or award traversal; overflow fails closed with `ERR_CORE_SETTLEMENT_COMMITMENT_COUNT`, `ERR_CORE_SETTLEMENT_WINNER_SLICE_COUNT`, or `ERR_CORE_SETTLEMENT_WINNER_COUNT`. |
| `LEGACY-GAP-2026-08-11-119` | Mode governance traversed unbounded participant, seat, and waitlist collections | 4 | T125 | RESOLVED | `DefaultGovernanceEngine` now bounds those collections before lookup/traversal with defaults of 256 participants, 64 seats, and 256 waitlist entries; overflow and full-list growth fail closed with `ERR_GOVERNANCE_PARTICIPANT_COUNT`, `ERR_GOVERNANCE_SEAT_COUNT`, or `ERR_GOVERNANCE_WAITLIST_COUNT`. |
| `LEGACY-GAP-2026-08-11-120` | Wizard preset/setup resolution and direct Game File compilation materialized unbounded collections or unsupported nested values | 4 | T126 | RESOLVED | `DefaultPresetResolver` and `DefaultGameFileCompiler` now apply bounded collection and canonical JSON checks before resolution, validation, or compilation; overflow fails closed with stable `ERR_WIZARD_*` codes. |
| `LEGACY-GAP-2026-08-11-121` | Direct receipt key-ring providers traversed unbounded retained signing or encryption key collections | 4 | T127 | RESOLVED | `ReceiptKeyRingSnapshot` and `StaticReceiptSigningKeyProvider` now enforce receipt-owned 128-entry retained-key limits before lookup traversal; oversized collections fail closed while active usable keys remain available. |
| `LEGACY-GAP-2026-08-11-122` | App persisted Hold'em session sources trusted a recovery store to bound event windows before suffix filtering and replay | 4 | T128 | RESOLVED | Mirrored app sources now enforce the shared 4,096-event default, with positive caller-owned limits, immediately after window load; oversized windows fail closed before snapshot decoding, identity provisioning, suffix materialization, or replay. |
| `LEGACY-GAP-2026-08-11-123` | App production persistence writers traversed caller event suffixes without an app-owned recovery event bound before append | 4 | T129 | RESOLVED | Mirrored persistence writers now enforce the shared 4,096-event default, with positive caller-owned limits, before event traversal, snapshot validation, or store append; oversized suffixes fail closed without durable writes. |
| `LEGACY-GAP-2026-08-11-124` | App session runtimes copied caller event batches before enforcing any app-owned batch bound | 4 | T130 | RESOLVED | Mirrored `AppTableSessionRuntime` owners now enforce the shared 4,096-event default, with positive caller-owned limits, before copying or reducing non-retention batches; oversized input fails closed with `ERR_APP_SESSION_EVENT_BATCH_TOO_LARGE` without state mutation. |
| `LEGACY-GAP-2026-08-11-125` | Production Hold'em composition did not propagate the T130 recovery-event limit beyond direct runtime construction | 4 | T131 | RESOLVED | Mirrored bootstrap, route registration, configuration, and session-factory seams now carry one validated limit into the app runtime; persisted configuration reuses it for source hydration and persistence-writer bounds, preventing recovery-path default drift. |
| `LEGACY-GAP-2026-08-11-126` | Accepted first-join and rejoin contexts had no app-shell seam to load the persisted production configuration at the typed handoff | 4 | T132 | RESOLVED | Mirrored shells now accept an optional `AppHoldemProductionSessionConfigurationLoader`, pass the accepted `JoinFlowSessionContext`, validate the returned configuration/writers and dynamic route, reuse the bootstrap adapter and native readiness gate, and fail closed on unavailable or unsafe results; product source/state/route policy remains caller-owned. |
| `LEGACY-GAP-2026-08-11-127` | Mirrored app shells had no direct registration path from the existing persisted production-session configuration factory to the typed join/rejoin loader | 4 | T134 | RESOLVED | Mobile and desktop runtimes now accept an optional `AppHoldemProductionSessionConfigurationFactory` and create a stable app-owned loader adapter when no explicit loader is supplied; explicit handlers/routes retain precedence, and product state, route policy, persistence, and database selection remain caller-owned. |
| `LEGACY-GAP-2026-08-11-128` | The persisted production-session factory returned event and snapshot writers, but the mounted Hold'em route discarded them and could not checkpoint accepted state | 4 | T135 | RESOLVED | Mirrored factories now share the existing event-plus-snapshot writer with a serialized route coordinator; local projection suffixes and remote accepted events are appended before typed snapshots, failed checkpoints remain ordered and retryable, and terminal retention clears pending state. Product source/state selection, database persistence, device validation, other-platform hosts, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-129` | Snapshot retry could drop a newer accepted checkpoint after repeated failure of an older checkpoint, and concurrent retries could replay stale state | 4 | T136 | RESOLVED | Mirrored mobile and desktop coordinators now retain newer checkpoints FIFO behind failed older retries, preserve durable event-suffix state, and resolve concurrent retry calls against the live queue so older state cannot overwrite newer accepted state. Product source/state selection, database persistence, device validation, other-platform hosts, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-130` | A late asynchronous production-session loader result could push a stale route after a newer join or route configuration had taken precedence | 4 | T137 | RESOLVED | Mirrored app shells now assign per-handoff generation tokens and ignore stale loader success/failure results after newer handoffs, disposal, or higher-precedence route configuration. Product source/state selection, database persistence, device validation, other-platform hosts, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-131` | Replacing or removing the optional production-session configuration factory did not invalidate a loader handoff created under the previous runtime contract | 4 | T138 | RESOLVED | Mirrored app shells now invalidate active generation tokens when the generated configuration loader factory is removed or replaced; delayed stale results cannot navigate after a widget/runtime rebuild. Product source/state selection, database persistence, device validation, other-platform hosts, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-132` | Android secure-key host validation counted UTF-16 characters instead of the locked UTF-8 byte limits used by the Dart contract and Windows host | 4 | T139 | RESOLVED | Android secure-key namespace and record validation now enforces UTF-8 byte bounds; shared channel-contract tests cover oversized multibyte material. Android device/runtime validation, cross-device networking, other-platform hosts, database persistence, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-133` | Dart secure-key method-channel requests did not bound namespace UTF-8 bytes before dispatch, while Android and Windows hosts enforced a 128-byte namespace limit | 4 | T140 | RESOLVED | Added the shared 128-byte secure-key namespace limit and Dart preflight rejection for oversized UTF-8 namespaces; focused method-channel tests verify the platform is not called. Android device/runtime validation, cross-device networking, other-platform hosts, database persistence, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-134` | Dart secure-key delete IDs and record fields did not consistently enforce host UTF-8/control-character validation before platform dispatch | 4 | T141 | RESOLVED | Added shared safe UTF-8 text validation for secure-key namespaces, IDs, purposes, algorithms, and secrets; oversized or control-bearing save/delete requests fail before native calls. Android device/runtime validation, cross-device networking, other-platform hosts, database persistence, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-135` | Generic native transport, discovery, capture, and app-storage decoders accepted padded or C0/C1-control-bearing text that the Android and Windows host boundaries reject | 4 | T142 | RESOLVED | Shared Dart bridge validation now rejects padded or C0/C1-control-bearing transport identities and receive scopes, local-network values, capture diagnostics, and app-storage paths/warnings before dispatch or policy projection. Other-platform hosts, device/runtime reachability, database persistence, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-136` | Android and Windows app-support directory hosts did not enforce the shared 4096-byte safe UTF-8 path boundary before returning a usable directory | 4 | T143 | RESOLVED | Android no-backup and Windows `LocalAppData` paths now fail closed when invalid, padded, C0/C1-control-bearing, or above 4096 UTF-8 bytes; the Windows smoke target asserts the returned path. Product database persistence, device/runtime validation, other-platform hosts, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-137` | The accepted join context reached the app configuration loader but was discarded before context-dependent production route policy could be composed | 4 | T144 | RESOLVED | Mirrored configuration factories now accept an optional `JoinFlowSessionContext` and context-aware route-policy factory; the loader forwards the exact accepted context while preserving the existing no-context fallback. Product state/database provisioning, device validation, other-platform hosts, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-138` | Production session configuration failures could discard recovery-store warnings after the store had been created | 4 | T145 | RESOLVED | Mirrored configuration factories now retain recovery-store warnings alongside the stable unavailable warning when route-policy or persisted-source composition fails. No exception detail or product state is exposed; product state/database provisioning, device validation, other-platform hosts, and release signing remain open. |
| `LEGACY-GAP-2026-08-11-139` | Direct pushes to retrofit and hardening branches did not run the repository production gates unless a pull request or default-branch push existed | 4 | T146 | RESOLVED | CI now runs on direct pushes to `retrofit/**` and `hardening/**` as well as `main`/`master`, and exposes manual `workflow_dispatch` execution. Existing analyze, boundary, source-text, test, dependency, Android, Windows, signing-guard, and native-smoke gates remain unchanged. |
| `LEGACY-GAP-2026-08-11-140` | A mounted production Hold'em surface could retain pending projection state across runtime replacement and accept late async completions from the previous session | 4 | T147 | RESOLVED | Mirrored production surfaces now reset pending projection, busy, and status state when runtime/coordinator/peer/local-seat identity changes; operation generations invalidate late persistence, transport, retry, and disposal completions. Focused mobile and desktop route tests cover delayed completion after runtime replacement. |
| `LEGACY-GAP-2026-08-11-141` | A late accepted inbound transport callback could reread replacement widget state and checkpoint or refresh the wrong mounted production route | 4 | T148 | RESOLVED | Mirrored table routes now capture the accepted event and owning runtime/coordinator under a lifecycle generation; callbacks from replaced or disposed transports are ignored before checkpointing or UI refresh. Focused mobile and desktop route tests cover delayed inbound completion after runtime replacement. |
| `LEGACY-GAP-2026-08-11-142` | Route cancellation stopped awaiting an in-flight native receive but the native frame drain could still deliver its late frames into the old runtime | 4 | T149 | RESOLVED | Mirrored native frame drains now accept source cancellation and race receive/handler work against it; native sessions forward route cancellation into the drain, so late receives fail closed before runtime delivery. Focused mobile and desktop transport/route tests cover cancellation suppression. |
| `LEGACY-GAP-2026-08-11-143` | Standalone transport-source disposal cancelled only the poll observer and did not propagate its lifecycle signal into the native drain | 4 | T150 | RESOLVED | Mirrored transport sources now expose an additive cancellable drain callback and complete its signal on source disposal or external cancellation; native session factories use it to prevent disposed sources from leaving native receives active. Focused mobile and desktop source/drain/session-factory tests cover disposal propagation. |
| `LEGACY-GAP-2026-08-11-144` | Transport provisioning could return an available native session/source when route cancellation was signaled during native session creation | 4 | T151 | RESOLVED | Mirrored transport provisioners now recheck cancellation after native session creation and before returning an available source; cancellation during creation fails closed with the existing stable session-load cancellation warning. Focused mobile and desktop provisioner/source/drain/session-factory tests cover the race. |
| `LEGACY-GAP-2026-08-11-145` | Caller-owned snapshot-ID factory exceptions escaped production checkpoint persistence as uncaught errors before the coordinator queue | 4 | T152 | RESOLVED | Mirrored production snapshot coordinators now invoke snapshot-ID factories inside the serialized checkpoint queue and return a stable persistence warning on failure without store mutation or pending state. Focused mobile and desktop snapshot coordinator tests cover the fail-closed path. |
| `LEGACY-GAP-2026-08-11-146` | Typed snapshot serialization could fail after event suffix append, leaving durable events without a checkpoint or escaping the persistence result contract | 4 | T153 | RESOLVED | Mirrored snapshot writers now preflight canonical serialization before event append and normalize serialization/hashing failures to stable persistence results; unsupported snapshot metadata leaves event and snapshot stores unchanged. Focused mobile and desktop persistence/snapshot writer tests cover the path. |
| `LEGACY-GAP-2026-08-11-147` | Snapshot coordinators copied event suffixes before enforcing the configured recovery-event limit, and configuration did not propagate its limit into the coordinator | 4 | T154 | RESOLVED | Mirrored snapshot coordinators now reject oversized suffixes before copying or persistence, and configuration factories pass one validated recovery limit into both coordinator and persistence writer. Focused mobile and desktop coordinator/configuration tests cover the bound. |
| `LEGACY-GAP-2026-08-11-148` | Snapshot coordinator retained every newer failed checkpoint during a persistent recovery-store outage, allowing unbounded in-memory growth | 4 | T155 | RESOLVED | Mirrored snapshot coordinators now retain at most 64 failed checkpoints by default, accept a positive caller-owned pending-checkpoint limit, and fail closed with a stable queue-full warning when full. Configuration factories propagate the limit, and focused mobile/desktop coordinator/configuration tests cover capacity and invalid limits. |
| `LEGACY-GAP-2026-08-11-149` | Native join bootstrap capped discovery input but copied unbounded reachable candidates returned by the injected provider into the accepted plan | 4 | T156 | RESOLVED | Mirrored native join bootstrap coordinators now cap reachable provider output at the configured peer limit while normalizing and deduplicating peer IDs before `BootstrapPlan` creation. Focused mobile and desktop native-bootstrap tests cover provider output capacity and normalization. |
| `LEGACY-GAP-2026-08-11-150` | Mirrored app transport drains validated receive scopes only for trimming and could pass control-bearing or oversized identities to an injected native bridge | 4 | T157 | RESOLVED | Mirrored native transport frame drains now reuse `NativeBridgePayloadLimits.isSafeUtf8Text` with the shared 256-byte transport identity limit before native receive. Focused mobile and desktop adapter suites cover C0, C1, and oversized scope rejection without native lookup. |
| `LEGACY-GAP-2026-08-11-151` | Mirrored app transport sinks validated only the trim-only network frame model before passing converted frames to injected native bridges | 4 | T158 | RESOLVED | Mirrored native transport frame sinks now require converted `NativeTransportFrame.isUsable` before bridge send, closing native UTF-8/control, identity, sequence, and payload invariant bypasses. Focused mobile and desktop adapter suites cover C0, C1, and oversized identity rejection without bridge calls. |
| `LEGACY-GAP-2026-08-11-152` | Mirrored app transport sources and provisioners accepted control-bearing or oversized scopes until a later drain/capability boundary | 4 | T159 | RESOLVED | Mirrored sources and provisioners now reuse `NativeBridgePayloadLimits.isSafeUtf8Text` with the shared 256-byte identity limit before source start, polling, or native capability lookup. Focused mobile and desktop suites cover lifecycle and provisioning rejection. |
| `LEGACY-GAP-2026-08-11-153` | Mirrored native-readiness loaders validated secure-key namespaces without the shared UTF-8 byte limit before bridge lookup | 4 | T160 | RESOLVED | Mirrored readiness loaders now require `NativeBridgePayloadLimits.isSafeUtf8Text` with the shared 128-byte secure-key namespace limit before storage lookup. Focused mobile and desktop readiness suites cover C1 and oversized namespace rejection without native calls. |
| `LEGACY-GAP-2026-08-11-154` | Mirrored Hold'em projection publishers validated peer identities only for trimming before handing canonical frames to generic/native senders | 4 | T161 | RESOLVED | Mirrored publishers now require `NativeBridgePayloadLimits.isSafeUtf8Text` with the shared 256-byte transport identity limit before sender calls. Focused mobile and desktop runtime suites cover C1 and oversized identity rejection without sender frames. |
| `LEGACY-GAP-2026-08-11-155` | Mirrored local identity and production-session composition validators accepted C1-bearing or UTF-8-byte oversized peer identities before the native transport boundary | 4 | T162 | RESOLVED | Mirrored local identity loaders/writers, persisted Hold'em route policies, and production-session factories now reuse `NativeBridgePayloadLimits.isSafeUtf8Text` with the shared 256-byte transport identity limit. Focused mobile and desktop suites cover persisted, generated, dynamic, and composed identity rejection before native save or route construction. |
| `LEGACY-GAP-2026-08-12-156` | Mirrored receipt key-ring loaders and writers accepted C1-bearing or UTF-8-byte oversized namespaces and relied on character/C0-only metadata checks around the native secure-key boundary | 4 | T163 | RESOLVED | Mirrored receipt key-ring loaders and writers now reuse the locked native secure-key UTF-8/C1 and byte limits for namespaces, key IDs, and secrets before bridge calls or receipt-key projection. Focused mobile and desktop suites cover C1 and byte-oversized namespace rejection without native mutation or load. |
| `LEGACY-GAP-2026-08-12-157` | Mirrored persisted Hold'em sources rejected empty recovery windows even when a product-owned initial typed snapshot was available, leaving first-join state provisioning outside the existing app source/configuration seam | 4 | T164 | RESOLVED | Mirrored sources now accept an optional product-owned initial snapshot loader, validate invite scope plus sequence/genesis invariants, ensure local identity, and checkpoint the initial typed state through the existing snapshot coordinator before returning app input. Focused mobile and desktop tests cover first-join persistence, invalid scope, missing coordinator, and checkpoint failure. |
| `LEGACY-GAP-2026-08-12-158` | Mirrored persisted Hold'em sources treated a recovery event suffix without a snapshot anchor as an empty first-join window, allowing initial product state to mask orphaned durable events | 4 | T165 | RESOLVED | Mirrored sources now fail closed when recovery events exist without a typed snapshot anchor, before initial-state loader, local identity, or checkpoint work. Focused mobile and desktop tests prove the orphaned suffix remains unchanged and no provisioning path is invoked. |
| `LEGACY-GAP-2026-08-12-159` | Mirrored persisted Hold'em sources decoded typed snapshot payloads without verifying the persisted envelope `snapshot_hash`, allowing tampered snapshot data to reach recovery state hydration | 4 | T166 | RESOLVED | Mirrored sources now recompute and verify the canonical snapshot payload hash before typed hydration; mismatches fail closed before identity provisioning or route input construction. Focused mobile and desktop tests cover tampered hash rejection while valid canonical test snapshots continue to load. |
| `LEGACY-GAP-2026-08-12-160` | Shared sync conflict planning and snapshot application accepted snapshot envelopes whose payload hash did not match `snapshot_hash`, leaving a package-level recovery bypass after app-source validation | 4 | T167 | RESOLVED | `BasicConflictDetector` and `BasicSnapshotApplier` now verify the canonical snapshot payload hash before recovery planning or projection and return `ERR_SNAPSHOT_PAYLOAD_HASH_MISMATCH` on failure. Shared sync tests cover detector/applier tamper rejection and canonical valid snapshots. |
| `LEGACY-GAP-2026-08-12-161` | Mirrored production table routes reread mutable `lastAcceptedEvent` state after an inbound callback instead of carrying the exact accepted event through the result contract | 4 | T168 | RESOLVED | Mirrored app session results now carry the accepted `EventEnvelope`; routes checkpoint that immutable callback result instead of rereading mutable runtime state. Focused mobile and desktop runtime and transport-handler tests cover the event identity fields. |
| `LEGACY-GAP-2026-08-12-162` | Mirrored app session and Hold'em inbound result objects exposed caller-owned warning lists that could be mutated after projection | 4 | T169 | RESOLVED | Mirrored result constructors now defensively copy and freeze warning diagnostics. Focused mobile and desktop runtime suites prove source-list isolation and `throwsUnsupportedError` on result mutation. |
| `LEGACY-GAP-2026-08-12-163` | Mirrored recovery-store and production-session configuration load results exposed caller-owned warning lists across the app startup/session boundary | 4 | T170 | RESOLVED | Mirrored recovery and production-session load-result constructors now defensively copy and freeze warning diagnostics. Focused mobile and desktop configuration, recovery, and app-shell suites cover source-list isolation and mutation rejection. |
| `LEGACY-GAP-2026-08-12-164` | Mirrored local-identity loader and provisioner results exposed caller-owned warning lists across the secure-key and production-session boundary | 4 | T171 | RESOLVED | Mirrored local-identity load and provision results now defensively copy and freeze warning diagnostics. Focused mobile and desktop identity suites cover source-list isolation and mutation rejection. |
| `LEGACY-GAP-2026-08-12-165` | Mirrored native-readiness snapshots and transport-source poll/start results exposed caller-owned warning lists across native readiness and live transport lifecycle boundaries | 4 | T172 | RESOLVED | Mirrored result constructors now defensively copy and freeze warning diagnostics. Focused mobile and desktop readiness and transport-source suites cover source-list isolation and mutation rejection. |
| `LEGACY-GAP-2026-08-12-166` | Mirrored native bootstrap, transport session/frame-drain, and receipt key-ring results exposed caller-owned collection diagnostics across app/native boundaries | 4 | T173 | RESOLVED | Mirrored result constructors now defensively copy and freeze bootstrap candidates, native receive results, and warning diagnostics. Focused mobile and desktop transport, bootstrap, and receipt suites cover source-list isolation and mutation rejection. |
| `LEGACY-GAP-2026-08-12-167` | Generic native bridge snapshots and transport frames exposed caller-owned discovery, secure-key, receive-frame, and payload collections across the package boundary | 4 | T174 | RESOLVED | Native bridge models now defensively copy and freeze discovery lists, secure-key records, receive frames, and frame payload bytes. Focused native bridge, mobile, and desktop suites cover source-list isolation and mutation rejection. |
| `LEGACY-GAP-2026-08-12-168` | Generic network models exposed caller-owned peer, discovery, transport-payload, warning, and ranking collections across the network package boundary | 4 | T175 | RESOLVED | Network models now defensively copy and freeze bootstrap peers/candidates, discovery values, transport payload bytes, warning diagnostics, and peer rankings. Focused network and mirrored mobile/desktop transport suites cover source-list isolation and mutation rejection. |
| `LEGACY-GAP-2026-08-12-169` | Sync and recovery request, window, conflict, snapshot, persistence, and reconciliation models exposed caller-owned event and diagnostic collections across the recovery boundary | 4 | T176 | RESOLVED | Sync models now defensively copy and freeze recovery events, conflicts, warnings, and reconciliation notes. Focused sync ownership, conflict, snapshot, and coordinator tests cover source-list isolation and mutation rejection. |

## Status Definitions

- RESOLVED: Closed by this additive retrofit baseline.
- OPEN: Actionable future implementation gap.
- DEFERRED: Known production-readiness work intentionally outside T1 scope.
