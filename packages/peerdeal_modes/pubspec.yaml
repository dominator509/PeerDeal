name: peerdeal_modes
description: PeerDeal mode adapters and session-policy contracts.
publish_to: none
version: 0.1.0
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  peerdeal_protocol:
    path: ../peerdeal_protocol
  peerdeal_core:
    path: ../peerdeal_core

dev_dependencies:
  test: ^1.25.2
  lints: ^4.0.0
