name: peerdeal_native_bridges
resolution: workspace
publish_to: none
description: PeerDeal native bridge shims for capture, secure storage, local network, and notifications.
version: 0.1.0

environment:
  sdk: '^3.11.5'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.30.0
