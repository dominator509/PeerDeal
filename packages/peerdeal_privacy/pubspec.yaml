name: peerdeal_privacy
description: PeerDeal privacy, retention, wipe, and metadata minimization starter scaffold.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  crypto: ^3.0.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0
