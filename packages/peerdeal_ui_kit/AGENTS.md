# peerdeal_ui_kit agent rules

## Owns
- shared Flutter widgets
- shared visual tokens
- reusable presentation helpers

## Do not do
- add game truth, reducers, or protocol schemas
- add app-specific navigation or session orchestration
- import sibling package `src/` internals

Keep this package reusable by both PeerDeal app shells.
