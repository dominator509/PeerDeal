name: peerdeal_receipts
resolution: workspace
description: PeerDeal receipt engine and binding-rules starter scaffold.
publish_to: none
version: 0.1.0

environment:
  sdk: '^3.11.5'
dependencies:
  peerdeal_protocol:
    path: ../peerdeal_protocol
  peerdeal_privacy:
    path: ../peerdeal_privacy
  meta: ^1.12.0

dev_dependencies:
  test: ^1.25.2
  lints: ^4.0.0
