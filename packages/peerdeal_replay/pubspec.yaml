name: peerdeal_replay
resolution: workspace
description: PeerDeal deterministic replay and anchor-validation starter scaffold.
publish_to: none
version: 0.1.0

environment:
  sdk: '^3.11.5'
dependencies:
  peerdeal_protocol:
    path: ../peerdeal_protocol
  peerdeal_core:
    path: ../peerdeal_core
  peerdeal_variants:
    path: ../peerdeal_variants
  peerdeal_modes:
    path: ../peerdeal_modes
  meta: ^1.17.0

dev_dependencies:
  test: ^1.30.0
  lints: ^6.1.0
