name: peerdeal_capture
description: Capture policy starter; real scaffold lands separately.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  meta: ^1.11.0

dev_dependencies:
  test: ^1.25.0
  lints: ^3.0.0
