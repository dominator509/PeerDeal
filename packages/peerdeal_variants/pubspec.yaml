name: peerdeal_variants
resolution: workspace
description: Hold'em-first variant adapter starter scaffold for PeerDeal.
version: 0.1.0
publish_to: none

environment:
  sdk: '^3.11.5'
dependencies:
  meta: ^1.12.0

dev_dependencies:
  lints: ^4.0.0
  peerdeal_core:
    path: ../peerdeal_core
  test: ^1.25.0
