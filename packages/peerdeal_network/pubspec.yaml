name: peerdeal_network
resolution: workspace
publish_to: none
description: PeerDeal network routing, LAN discovery, relay fallback, and bootstrap abstractions.
version: 0.1.0

environment:
  sdk: '^3.11.5'
dependencies:
  peerdeal_protocol:
    path: ../peerdeal_protocol

dev_dependencies:
  test: ^1.25.2
  lints: ^4.0.0
