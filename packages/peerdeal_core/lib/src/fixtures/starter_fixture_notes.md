# Starter fixtures

These starter fixtures are intentionally small and deterministic.
They exist to prove package-local reducer, validation, and invariant behavior before the full protocol and hand lifecycle are wired in.
