name: peerdeal_desktop
resolution: workspace
description: PeerDeal desktop app shell starter.
publish_to: none
version: 0.1.0

environment:
  sdk: '^3.11.5'
dependencies:
  flutter:
    sdk: flutter
  peerdeal_capture: ^0.1.0
  peerdeal_native_bridges: ^0.1.0
  peerdeal_privacy: ^0.1.0
  peerdeal_protocol: ^0.1.0
  peerdeal_receipts: ^0.1.0
  peerdeal_sync: ^0.1.0

dev_dependencies:
  test: ^1.25.0
