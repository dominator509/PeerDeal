name: peerdeal_desktop
resolution: workspace
description: PeerDeal desktop app shell starter.
publish_to: none
version: 0.1.0

environment:
  sdk: '^3.11.5'
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  test: ^1.25.0
